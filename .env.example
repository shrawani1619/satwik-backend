# Copy to .env and fill in values. Never commit .env.

PORT=8080
NODE_ENV=development

DB_URI=
JWT_SECRET=
JWT_EXPIRE=7d

# Comma- or space-separated browser origins (no trailing slash).
# Merged with defaults in app.js — add preview URLs, staging, etc. without code changes.
# CORS_ORIGINS=https://satwik-frontend.vercel.app,https://your-preview.vercel.app
